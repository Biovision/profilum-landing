<div class="data">
  <div><%= admin_age_group_link(entity) %></div>
  <div class="secondary info">
    <%= t(:program_count, count: entity.programs_count) %>
  </div>

  <ul class="actions">
    <li><%= edit_icon edit_age_group_path(entity.id) %></li>
    <%= render partial: 'shared/actions/priority_changer', locals: { path: priority_admin_privilege_path(entity.id) } %>
  </ul>
</div>
