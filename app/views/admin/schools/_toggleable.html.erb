<div class="toggleable" data-url="<%= toggle_admin_school_path(entity) %>">
  <% School.toggleable_attributes.each do |flag| %>
  <span class="<%= entity.attributes[flag.to_s] ? 'active' : 'inactive' %>" data-flag="<%= flag %>">
    <%= t("activerecord.attributes.school.#{flag}") %>
  </span>
  <% end %>
</div>
