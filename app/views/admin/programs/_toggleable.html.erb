<div class="toggleable" data-url="<%= toggle_admin_program_path(entity) %>">
  <% Program.toggleable_attributes.each do |flag| %>
  <span class="<%= entity.attributes[flag.to_s] ? 'active' : 'inactive' %>" data-flag="<%= flag %>">
    <%= t("activerecord.attributes.program.#{flag}") %>
  </span>
  <% end %>
</div>
