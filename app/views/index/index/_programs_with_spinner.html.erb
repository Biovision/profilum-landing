<% if collection.any? %>
    <section class="programs" id="program-previews">
      <ul data-page="<%= collection.current_page %>">
        <% collection.each do |entity| %>
            <li>
              <%= render partial: 'programs/preview', locals: { entity: entity } %>
            </li>
        <% end %>
      </ul>

      <div class="loader">
        <div class="spinner <%= collection.next_page.blank? ? 'hidden' : '' %>">
          <%= image_tag('profilum/landing/icons/loader.svg') %>
        </div>
      </div>
    </section>
<% end %>
